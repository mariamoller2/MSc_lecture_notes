class: center, middle

<img src="https://apriko.com/en/wp-content/uploads/sites/3/2024/12/devops.png" width=50%/>

# DevOps, Software Evolution and Software Maintenance

Helge Pfeiffer, Associate Professor,<br>
[Research Center for Government IT](https://www.itu.dk/forskning/institutter/institut-for-datalogi/forskningscenter-for-offentlig-it),<br>
[IT University of Copenhagen, Denmark](https://www.itu.dk)<br>
`ropf@itu.dk`

---

class: center, middle

# Feedback: The state of your projects?

---

### Release Activity

<object width="100%" data="http://209.38.211.172/release_activity_weekly.svg"></object>

---

### Weekly Commit Activity

<object width="100%" data="http://209.38.211.172/commit_activity_weekly.svg"></object>

---

### Latest processed events?

<object width="100%" data="http://64.226.108.122/chart.svg"></object>

---

### Error plot

<object width="100%" data="http://64.226.108.122/error_chart.svg"></object>

---

## How do you feel it is going with your projects?

---

## Feedback Monitoring


- So far, only 8/13 groups have handed in a URL to monitoring dashboards which are accessible to us?
  → Please fix that!
<!--
b, c, d, f, i, k, l, m
 -->
- For three groups: I cannot login? → Please fix that!
<!--
group e, g, and j
 -->

<img src="images/group_e_error.png" width="80%">
<!--
<img src="images/group_g_error.png" width="50%">
<img src="images/group_j_error.png" width="50%">
 -->

---

## Feedback Monitoring


Remember:

  > Without monitoring you are not doing your job.
  >
  > James Turnbull _"The Art of Monitoring"_

So everybody, please add monitoring to your applications and share the link to the dashboard with us.

--

### Please remember that the monitoring dashboard has to be accessible for us...

Either it is public or you have to use the [credentials were distributed via Teams](https://teams.cloud.microsoft/l/channel/19%3ANtbHJgbpONeG3FiF2DS0jX2R_qNAcnzAJr8bZ0qtAVA1%40thread.tacv2/General?groupId=dccde382-03f7-4b4f-bbe1-252d63a8b953&tenantId=bea229b6-7a08-4086-b44c-71f57f716bdb).

---

## Feedback Monitoring

Some quite bare bone monitoring dashboards of only certain aspects of your systems:

<table>
  <tr>
    <td>
      <img src="images/group_b_monitoring.png" width="100%">
    </td>
    <td>
      <img src="images/group_i_monitoring.png" width="100%">
    </td>
  </tr>
  <tr>
    <td>
      <img src="images/group_d_monitoring.png" width="100%">
    </td>
    <td>
      <img src="images/group_d_monitoring2.png" width="100%">
    </td>
  </tr>
</table>

---

## Feedback Monitoring

Some quite bare bone monitoring dashboards of only certain aspects of your systems

→ Please expand that!

  > From a technology perspective, monitoring is the tools and processes by which you measure and manage your IT systems. But monitoring is much more than that. Monitoring provides the translation between business value and the metrics generated by your systems and applications. Your monitoring system translates those metrics into a measurable user experience. That measurable user experience provides feedback to the business to help ensure it’s delivering what customers want. The user experience also provides feedback to IT to indicate what isn’t working and what’s delivering insufficient quality of service.
  >
  > Your monitoring system has two customers:
  >
  >   * The business
  >
  >   * Information Technology
  >
  > James Turnbull _"The Art of Monitoring"_

---

## Feedback Monitoring

Some quite elaborate and multi-aspect dashboards!

<table>
  <tr>
    <td>
<img src="images/group_c_monitoring.png" width="100%">
    </td>
    <td>
<img src="images/group_c_monitoring2.png" width="100%">
    </td>
  </tr>
  <tr>
    <td>
<img src="images/group_f_monitoring.png" width="100%">
    </td>
    <td>
<img src="images/group_f_monitoring2.png" width="100%">
    </td>
  </tr>
</table>

---

## Feedback Monitoring

Some quite elaborate and multi-aspect dashboards!

<table>
  <tr>
    <td>
<img src="images/group_f_monitoring3.png" width="100%">
    </td>
    <td>
<img src="images/group_k_monitoring.png" width="100%">
    </td>
  </tr>
  <tr>
    <td>
<img src="images/group_k_monitoring2.png" width="100%">
    </td>
    <td>
<img src="images/group_k_monitoring3.png" width="100%">
    </td>
  </tr>
</table>

---

## Feedback Monitoring

Some quite elaborate and multi-aspect dashboards!

<table>
  <tr>
    <td>
<img src="images/group_l_monitoring.png" width="100%">
    </td>
    <td>
<img src="images/group_l_monitoring2.png" width="100%">
    </td>
  </tr>
  <tr>
    <td>
<img src="images/group_m_monitoring.png" width="100%">
    </td>
    <td>
<img src="images/group_m_monitoring2.png" width="100%">
    </td>
  </tr>
</table>

---

## Feedback Monitoring

Some quite elaborate and multi-aspect dashboards!

<table>
  <tr>
    <td>
<img src="images/group_m_monitoring4.png" width="100%">
    </td>
    <td>
<img src="images/group_m_monitoring5.png" width="100%">
    </td>
  </tr>
  <tr>
    <td>
<img src="images/group_m_monitoring6.png" width="100%">
    </td>
    <td>
<img src="images/group_m_monitoring7.png" width="100%">
    </td>
  </tr>
</table>

---

## Feedback Monitoring

Some quite elaborate and multi-aspect dashboards!

<table>
  <tr>
    <td>
<img src="images/group_m_monitoring8.png" width="100%">
    </td>
    <td>
<img src="images/group_m_monitoring9.png" width="100%">
    </td>
  </tr>
</table>

---

## Feedback Logging

- So far, only 8/13 groups have handed in a URL to monitoring dashboards which are accessible to us?
  → Please fix that!
<!--
b, c, d, f, i, k, l, m
 -->
- For three groups: I cannot login? → Please fix that!
<!--
group e, g, and j
 -->

<img src="images/group_e_error.png" width="80%">

---

## Feedback Logging

- Some quite bare bone logs, logging of only certain aspects of your systems.
- Only from one container? You have more components in your systems!

→ Please expand that!

<table>
  <tr>
    <td>
<img src="images/group_b_logging.png" width="100%">
    </td>
    <td>
<img src="images/group_c_logging.png" width="100%">
    </td>
  </tr>
</table>

---

## Feedback Logging

Remember for logs, their origin must be clear!
For example, where do these come from? Application, OS, etc.?

<table>
  <tr>
    <td>
<img src="images/group_f_logging.png" width="100%">
    </td>
    <td>
<img src="images/group_f_logging2.png" width="100%">
    </td>
  </tr>
</table>

---

## Feedback Logging

Some quite nice multi-source logs!

<table>
  <tr>
    <td>
<img src="images/group_d_logging.png" width="100%">
    </td>
    <td>
<img src="images/group_i_logging2.png" width="100%">
    </td>
  </tr>
  <tr>
    <td>
<img src="images/group_i_logging3.png" width="100%">
    </td>
    <td>
<img src="images/group_k_logging.png" width="100%">
    </td>
  </tr>
</table>

---

## Feedback Logging

Some quite nice multi-source logs!

<table>
  <tr>
    <td>
<img src="images/group_l_logging.png" width="100%">
    </td>
    <td>
<img src="images/group_m_logging.png" width="100%">
    </td>
  </tr>
  <tr>
    <td>
<img src="images/group_m_logging3.png" width="100%">
    </td>
    <td>
<img src="images/group_m_logging4.png" width="100%">
    </td>
  </tr>
</table>

---

## Feedback Logging: @all!

Remember what logs are?

  > Logs are
  > * _streams_ of
  > * _aggregated_
  > * _time-ordered_ events
  > * collected from **all running processes and backing services**
  >
  > [A. Wiggins _"The twelve-factor app"_](https://12factor.net/logs)

→ Please expand that!

---

class: center, middle

# Scaling Revisited


---

## What did we do the last time?

High-availability setup with redundant load balancer

<img src="https://doimages.nyc3.cdn.digitaloceanspaces.com/ha-diagram-animated.gif" width="100%">

Image source: https://www.digitalocean.com/blog/floating-ips-start-architecting-your-applications-for-high-availability

---

## What did we do the last time?

[Scripted scaling](https://github.com/itu-devops/scaling_exercises/blob/basic_scaling/scale_app.sh) of services behind a load balancer.

<img src="./images/scaling_diag.png" width="90%">

--

What is the problem with this setup?

--

- It does not scale 😀
- It works well for scaling one system, i.e., one binary.
- But what if your application consists of many components that need each to be scaled?

---

## Alternative: Container Orchestration Tools

Container orchestration tools...
1. manage computing nodes and services
2. schedule tasks in a resource aware manner


<img src="https://hackernoon.imgix.net/images/dYqUMFe1hKR6HT003tVDYU73ayW2-ny93r3y.jpeg" width="50%">



- [Docker Engine Swarm Mode](https://docs.docker.com/engine/swarm/)
- [Nomad](https://developer.hashicorp.com/nomad)
- [Kubernetes](https://kubernetes.io/)
- [OpenShift](https://www.redhat.com/en/technologies/cloud-computing/openshift)
- ...

<tiny>Image source: <a href="https://hackernoon.com/container-orchestration-smackdown-kubernetes-vs-docker-swarm-vs-nomad">https://hackernoon.com/container-orchestration-smackdown-kubernetes-vs-docker-swarm-vs-nomad</a></tiny>

---

## Container Orchestration Tools

- [Docker Engine Swarm Mode](https://docs.docker.com/engine/swarm/)
  - Integral part of the Docker engine
  - It is already installed on your computers in the default Docker engine installation
  - Likely, the most straight-forward tool to use compared to the other alternatives
- [Nomad](https://developer.hashicorp.com/nomad)
  - A tool by HashiCorp (the same company behind Vagrant, Terraform, etc.)
  - _"A scheduler and orchestrator to deploy and manage containers and non-containerized applications [...] at scale"_
  - manage containers and non-containerized ← that is _the_ key feature
- [Kubernetes](https://kubernetes.io/)
  - Originally developed by Google for Google scale problems
  - Likely [industry standard](https://www.jobindex.dk/jobsoegning?q=kubernetes) [at the moment](https://www.linkedin.com/jobs/search?keywords=Kubernetes&location=Denmark&geoId=104514075&trk=public_jobs_jobs-search-bar_search-submit&position=1&pageNum=0)
  - > Kubernetes has to be most complex software I've ever tried to learn. I eventually gave up and decided to stick with simple single machine docker-compose deployments. I figure by the time any of my personal projects actually need to scale beyond 1 machine, I'd probably have enough revenue that I can afford to hire someone else to worry about it.
    >
    > [HackerNews Discussion](https://news.ycombinator.com/item?id=26271470)

<!--
- [OpenShift](https://www.redhat.com/en/technologies/cloud-computing/openshift)
  - Developed by Red Hat as an enterprise Kubernetes distribution
  - Provides advanced networking features and built-in monitoring tools
 -->

---

## Docker Engine Swarm Mode

> Current versions of Docker include Swarm mode for natively managing a cluster of Docker Engines called a swarm. Use the Docker CLI to create a swarm, deploy application services to a swarm, and manage swarm behavior.
>
> Docker Swarm mode is built into the Docker Engine.
>
> [Docker Engine Documentation](https://docs.docker.com/engine/swarm/)

---

## Docker Engine Swarm Mode - Main Concepts

The following [concepts are essential for understanding Docker Swarm mode](https://docs.docker.com/engine/swarm/key-concepts/#what-is-a-swarm)

1. **Swarm/Cluster**: Group of nodes that work together to create a distributed system
2. **Node**: A (virtual) machine participating in a swarm
   - **Manager**
   - **Worker**
3. **Service**: _"the image for a [component] within the context of some larger application."_
4. **Task**: An instance of a process
5. **Routing Mesh**: Network overlay mechanism allowing for load balancing

They have equivalents in other orchestration environments.

---

### 1. Manager Node

<img src="https://docs.docker.com/engine/swarm/images/swarm-diagram.webp" width="100%">

  * Maintain swarm (cluster) state
  * Schedule services on the nodes
  * Designed for redundancy

--

> An odd number N of manager nodes in the cluster tolerates the loss of at most (N-1)/2 managers. Docker recommends a maximum of seven manager nodes for a swarm.
>
> Important: Adding more managers does NOT mean increased scalability or higher performance. In general, the opposite is true.
>
> [Docker Engine Documentation](https://docs.docker.com/engine/swarm/how-swarm-mode-works/nodes/#manager-nodes)

<tiny>Image source: <a href="https://docs.docker.com/engine/swarm/how-swarm-mode-works/nodes">https://docs.docker.com/engine/swarm/how-swarm-mode-works/nodes</a></tiny>

---

### 2. Worker Node

<img src="https://docs.docker.com/engine/swarm/images/swarm-diagram.webp" width="100%">

 - [_"whose sole purpose is to execute containers"_](https://docs.docker.com/engine/swarm/how-swarm-mode-works/nodes/#worker-nodes)
 - A swarm has 1..+ manager nodes and 0..* worker nodes
 - By default, managers are also workers
 - Workers can be promoted to managers and vice versa


<tiny>Image source: <a href="https://docs.docker.com/engine/swarm/how-swarm-mode-works/nodes">https://docs.docker.com/engine/swarm/how-swarm-mode-works/nodes</a></tiny>

---

### 3. Service

<!--
<img src="https://docs.docker.com/engine/swarm/images/services-diagram.webp" style="width: 40%; float:right; margin: 10px;">
 -->

<img src="https://docs.docker.com/engine/swarm/images/services-diagram.webp" width="50%">

> When you create a service, you specify which container image to use and which commands to execute inside running containers.
> You also define options for the service:
>
>   - The **port** where the swarm makes the service available outside the swarm
>   - An **overlay network** for the service to connect to other services in the swarm
>   - **CPU and memory limits** and reservations
>   - A **rolling update policy**
>   - The **number of replicas** of the image to run in the swarm
>
> [Docker Engine Documentation](https://docs.docker.com/engine/swarm/how-swarm-mode-works/services/)

<tiny>Image source: <a href="https://docs.docker.com/engine/swarm/how-swarm-mode-works/services/">https://docs.docker.com/engine/swarm/how-swarm-mode-works/services/</a></tiny>

---

### 4. Task

<img src="https://docs.docker.com/engine/swarm/images/service-lifecycle.webp" style="width: 40%; float:right; margin: 10px;">


- [_"the atomic unit of scheduling within a swarm"_](https://docs.docker.com/engine/swarm/how-swarm-mode-works/services/#tasks-and-scheduling): When defining a service, the orchestrator creates tasks to fulfill the desired state.
- **Task states**:
  - ASSIGNED → PREPARING → RUNNING
  - If a task fails, the orchestrator removes it and creates a new replacement task rather than restarting the failed one.
- **Each task is a slot**:
  - The scheduler fills it by spawning a container.
  - A container is the actual running instance of a task.

--

Think of _service_ is a description of a desired state, and a _task_ performs the work.

<tiny>Image source: <a href="https://docs.docker.com/engine/swarm/how-swarm-mode-works/services/">https://docs.docker.com/engine/swarm/how-swarm-mode-works/services/</a></tiny>

---

#### Types of Services

There are two types of service deployments:

- **replicated**: allows you to select the level of replication
- **global**: a service that runs one task on every node.

<img src="https://docs.docker.com/engine/swarm/images/replicated-vs-global.webp" width="50%">

<tiny>Image source: <a href="https://docs.docker.com/engine/swarm/how-swarm-mode-works/services/">https://docs.docker.com/engine/swarm/how-swarm-mode-works/services/</a></tiny>

--

Questions:

- What service does it make sense to have "global" replication for?
- What services should not be replicated, i.e., replication level = 1?

<!--

Good examples of global service? A **log shipper** or a **monitoring container**. It is important to ensure that the service is running on every node. By deploying the service as a global service, you can ensure that every node in the cluster has a copy of the service running, which can collect data from that node and forward it to a centralized location.
 -->

---

### 5. The Routing Mesh


> Docker Routing Mesh is a built-in feature in Docker Swarm that allows services to be accessible from any node in the cluster, even if the service's container isn't running on that node. It automatically load balances incoming requests across available service replicas, simplifying service accessibility and management.
>
> [Docker Engine Documentation](https://docs.docker.com/engine/swarm/ingress/)


<img src="https://docs.docker.com/engine/swarm/images/ingress-routing-mesh.webp" width="80%">


<!--
> All nodes participate in an ingress routing mesh. The routing mesh enables each node in the swarm to accept connections on published ports for any service running in the swarm, even if there's no task > running on the node. The routing mesh routes all incoming requests to published ports on available nodes to an active container.
>
> To use the ingress network in the swarm, you need to have the following ports open between the swarm nodes before you enable Swarm mode:
>
>   * Port 7946 TCP/UDP for container network discovery.
>   * Port 4789 UDP (configurable) for the container ingress network.
>
> When setting up networking in a Swarm, special care should be taken. Consult the tutorial for an overview.
>
> You must also open the published port between the swarm nodes and any external resources, such as an external load balancer, that require access to the port.
>
> [Docker Engine Documentation](https://docs.docker.com/engine/swarm/ingress/)

 -->

<tiny>Image source: <a href="https://docs.docker.com/engine/swarm/ingress/">https://docs.docker.com/engine/swarm/ingress/</a></tiny>

---

### Deploying to a Docker Swarm: A Single Service

> Swarm services use a declarative model, which means that you define the desired state of the service, and rely upon Docker to maintain this state. The state includes information such as (but not limited to):
>
>   * The image name and tag the service containers should run
>   * How many containers participate in the service
>   * Whether any ports are exposed to clients outside the swarm
>   * Whether the service should start automatically when Docker starts
>   * The specific behavior that happens when the service is restarted (such as whether a rolling restart is used)
>   * Characteristics of the nodes where the service can run (such as resource constraints and placement preferences)
>
> [Docker Engine Documentation](https://docs.docker.com/engine/swarm/how-swarm-mode-works/services/)

```bash
docker service create --name my_web \
                      --replicas 3 \
                      --publish published=8080,target=80 \
                      nginx
```

---

### Deploying to a Docker Swarm: An Application Stack

You can deploy, orchestrate, and scale multi-container (multi-service) applications with `docker stack deploy`.
This replaces `docker compose up`.

```bash
$ docker stack deploy -c <docker-compose>.yml <application_stack_name>
```

- Note, [`docker stack deploy`](https://docs.docker.com/reference/compose-file/legacy-versions/) and [`docker compose up`](https://docs.docker.com/reference/compose-file/) use different versions of `docker-compose.yml` files.
- Key differences between `docker stack deploy` and `docker compose`:
  - `docker stack` **ignores** `build` attribute, i.e., images must be built and pushed to a registry before deployment
  - `docker stack` **reads** the `deploy` attribute (`docker compose` ignores it)
  - Stacks are managed as a unit:
     *  `docker stack ls`
     *  `docker stack services <application_stack_name>`
     *  `docker stack ps <application_stack_name>`
     *  `docker stack rm <application_stack_name>`

See:
  - [Docker Engine Documentation _"Deploy a stack to a swarm"_](https://docs.docker.com/engine/swarm/stack-deploy/)
  - [The Difference Between Docker Compose And Docker Stack](https://vsupalov.com/difference-docker-compose-and-docker-stack/)

---

#### `docker stack` `deploy` Attribute

Extra information on how to deploy a service/stack to a swarm, are detailed under the `deploy` attribute:

- [`mode`](https://docs.docker.com/reference/compose-file/deploy/#mode)
- [`replicas`](https://docs.docker.com/reference/compose-file/deploy/#replicas)
- [`placement`](https://docs.docker.com/reference/compose-file/deploy/#placement)
- [`resources`](https://docs.docker.com/reference/compose-file/deploy/#resources)
- [`restart_policy`](https://docs.docker.com/reference/compose-file/deploy/#restart_policy)
- [`update_config`](https://docs.docker.com/reference/compose-file/deploy/#update_config)

Example:

```yaml
services:
  api:
    image: itudevops/minitwit
    deploy:
      replicas: 2
      placement:
        constraints:
          - "node.hostname!=dbvm"
          - "node.label==frankfurt"
      update_config:
        delay: 10s
        order: start-first
```

---

### Deploying to a Docker Swarm: An Application Stack

```yaml
volumes:
    mysql_data:

services:
    visualizer:
        image: dockersamples/visualizer:stable
        ports:
            - "8888:8080"
        volumes:
            - "/var/run/docker.sock:/var/run/docker.sock"
        deploy:
            placement:
                constraints: [node.role == manager]

    minitwit:
        image: youruser/minitwitimage:latest
        ports:
            - "5001:5000"
        deploy:
            replicas: 2

    itusqlimage:
        image: mariadb:10.6
        ports:
            - "3306:3306"
        environment:
            - MYSQL_ROOT_PASSWORD=megetsikkertkodeord
        volumes:
            - mysql_data:/var/lib/mysql
            - ./docker/minitwit/minitwit_init.sql:/docker-entrypoint-initdb.d/init.sql
        deploy:
            replicas: 1
            placement:
                constraints: [node.role == manager]
```

See your exercise example: https://github.com/itu-devops/itu-minitwit-docker-swarm/blob/local_swarm/minitwit_stack.yml

---

### Deploying & Scaling to a Docker Swarm Exercise

```bash
vagrant ssh swarm-manager-1 -c "docker stack deploy -c /vagrant/minitwit_stack.yml minitwit"
vagrant ssh swarm-manager-1 -c "docker service scale minitwit_minitwit=10"
```

<table>
  <tr>
  <td>
    <img src="images/docker_swarm_viz.png" width="70%">
  </td>
  <td>
    <img src="images/docker_swarm_scaled_viz.png" width="80%">
  </td>
  </tr>
</table>

See: [Exercise Task 1](https://github.com/itu-devops/itu-minitwit-docker-swarm/blob/local_swarm/README.md#task-1-deployment-and-scaling-of-the-application)

---

### Scaling a Docker Swarm Exercise

```bash
create_new_swarm_worker.sh
ssh new-swarm-worker
```

```bash
docker swarm join --token <token> 192.168.20.6:2377
```

See: [Exercise Task 2](https://github.com/itu-devops/itu-minitwit-docker-swarm/blob/local_swarm/README.md#task-2-scaling-of-the-docker-swarm-cluster)

---

### Upgrading a Service on a Docker Swarm

- [`update_config`](https://docs.docker.com/reference/compose-file/deploy/#update_config)
- Two `order` options:
  - `stop-first` (default): corresponds to rolling updates
  - `start-first`: corresponds to blue-green service deployment
- [Rolling Updates Swarm Tutorial](https://docs.docker.com/engine/swarm/swarm-tutorial/rolling-update/ )


Example:

```yaml
services:
  api:
    image: itudevops/minitwit
    deploy:
      replicas: 2
      placement:
        constraints:
          - "node.hostname!=dbvm"
          - "node.label==frankfurt"
      update_config:
        delay: 10s
        order: start-first
```

---

#### Kubernetes

Docker Swarm Mode is already quite complex: Swarm/Cluster, Manager Nodes, Worker Nodes, Services, Tasks, Routing Mesh, Stacks, etc.

--

Kubernetes, the industry standard to solve Google size problems is more complex:

<img src="https://miro.medium.com/v2/resize:fit:720/format:webp/1*S4hNwaDv4kxTR1TtmIOe1A.gif" width="80%">

<tiny>Image source: <a href="https://medium.com/@vikwaso/kubernetes-architecture-a-deep-dive-into-the-core-components-b93a953019d1">https://medium.com/@vikwaso/kubernetes-architecture-a-deep-dive-into-the-core-components-b93a953019d1</a></tiny>

---

#### Example, _ITU-MiniTwit_ on a Local Docker Swarm

Definition of a cluster of local virtual machine nodes in [`Vagrantfile`](https://github.com/itu-devops/itu-minitwit-docker-swarm/blob/local_swarm/Vagrantfile)

```ruby
Vagrant.configure("2") do |config|
  config.vm.box = "bento/ubuntu-22.04"

  # Define the number of swarm nodes
  MANAGER_COUNT = 1
  WORKER_COUNT = 2
  BASE_IP = "192.168.20"
  IP_START = 6

  # Manager nodes
  (1..MANAGER_COUNT).each do |i|
    config.vm.define "swarm-manager-#{i}" do |manager|
      manager.vm.hostname = "swarm-manager-#{i}"
      manager.vm.network "private_network", ip: "#{BASE_IP}.#{IP_START + i - 1}"

      manager.vm.provider "virtualbox" do |vb|
        vb.memory = 2048
        vb.cpus = 2
        vb.name = "swarm-manager-#{i}"
      end

      # Provision with Docker and initialize swarm
      manager.vm.provision "shell", path: "provisioners/install_docker.sh"
      manager.vm.provision "shell", path: "provisioners/configure_firewall_manager.sh"
      manager.vm.provision "shell", path: "provisioners/init_swarm_manager.sh", args: "#{BASE_IP}.#{IP_START + i - 1}"
      manager.vm.provision "shell", path: "provisioners/build_app_image.sh"
    end
  end

  # Worker nodes
  (1..WORKER_COUNT).each do |i|
    config.vm.define "swarm-worker-#{i}" do |worker|
      worker.vm.hostname = "swarm-worker-#{i}"
      worker.vm.network "private_network", ip: "#{BASE_IP}.#{IP_START + MANAGER_COUNT + i - 1}"

      worker.vm.provider "virtualbox" do |vb|
        vb.memory = 2048
        vb.cpus = 2
        vb.name = "swarm-worker-#{i}"
      end

      # Provision with Docker
      worker.vm.provision "shell", path: "provisioners/install_docker.sh"
      worker.vm.provision "shell", path: "provisioners/configure_firewall_worker.sh"
      worker.vm.provision "shell", path: "provisioners/join_swarm_worker.sh"
    end
  end
end
```

---

class: center, middle

# Infrastructure as Code (IaC) Revisited

---

## Do you remember session 03?

  > Topic of today: What about that server?

<img src="../session_03/images/01_scp2.png" width=50%>

  * Where does the server come from?
  * How does the server come there?

---

### Infrastructure as Code (IaC)

> What is the purpose of all these Vagrantfiles or scripted creation of virtual machines?
>
> → We want to encode our infrastructure in executable artifacts so that we can recreate it whenever needed.
> This is usually called _Infrastructure as Code_ (IaC).
>
> Later in the course, we will have a look at a more advanced tool for IaC, i.e., Terraform.
> For now, we just want to make sure to encode creation of our infrastructure as code in a reasonable way.
>
> [Helge in Session 03](../session_03/Slides.md)

---

### How did we Specify IaC?

For example, via (shell) scripts or Vagrantfiles.

```python
import digitalocean
from os import getenv


manager = digitalocean.Manager(token=getenv("DIGITAL_OCEAN_TOKEN"))
my_droplets = manager.get_all_droplets()
droplet = [d for d in my_droplets if d.name == "myFstVm"][0]
droplet.destroy()
```

--

```ruby
Vagrant.configure("2") do |config|
  config.vm.box = "bento/ubuntu-22.04"

  # Define the number of swarm nodes
  MANAGER_COUNT = 1
  WORKER_COUNT = 2
  BASE_IP = "192.168.20"
  IP_START = 6

  # Manager nodes
  (1..MANAGER_COUNT).each do |i|
    config.vm.define "swarm-manager-#{i}" do |manager|
      manager.vm.hostname = "swarm-manager-#{i}"
      manager.vm.network "private_network", ip: "#{BASE_IP}.#{IP_START + i - 1}"

      manager.vm.provider "virtualbox" do |vb|
        vb.memory = 2048
        vb.cpus = 2
        vb.name = "swarm-manager-#{i}"
      end

      # Provision with Docker and initialize swarm
      manager.vm.provision "shell", path: "provisioners/install_docker.sh"
      manager.vm.provision "shell", path: "provisioners/configure_firewall_manager.sh"
      manager.vm.provision "shell", path: "provisioners/init_swarm_manager.sh", args: "#{BASE_IP}.#{IP_START + i - 1}"
      manager.vm.provision "shell", path: "provisioners/build_app_image.sh"
    end
  end

  # Worker nodes
  (1..WORKER_COUNT).each do |i|
    config.vm.define "swarm-worker-#{i}" do |worker|
      worker.vm.hostname = "swarm-worker-#{i}"
      worker.vm.network "private_network", ip: "#{BASE_IP}.#{IP_START + MANAGER_COUNT + i - 1}"

      worker.vm.provider "virtualbox" do |vb|
        vb.memory = 2048
        vb.cpus = 2
        vb.name = "swarm-worker-#{i}"
      end

      # Provision with Docker
      worker.vm.provision "shell", path: "provisioners/install_docker.sh"
      worker.vm.provision "shell", path: "provisioners/configure_firewall_worker.sh"
      worker.vm.provision "shell", path: "provisioners/join_swarm_worker.sh"
    end
  end
end
```

---

### How did we Specify IaC?

For example, via (shell) scripts or Vagrantfiles.

What is the problem with these?

--

- Remember Idempotent Shell Scripts?
  ![](../session_05/images/idempotent.png)


--


- (Shell) scripts and Vagrantfiles are written in imperative programming languages
- Likely, interrupting an imperative program, leaves infrastructure in undefined state

--


- Even though you have scripts, likely you mutated your infrastructure manually after creation since scripts were brittle.


--

→ Currently, we have mutable, non-idempotent, imperative IaC

---

### Mutable vs. Immutable Infrastructure?


> An immutable infrastructure is another infrastructure paradigm in which servers are never modified after they’re deployed. If something needs to be updated, fixed, or modified in any way, new servers built from a common image with the appropriate changes are provisioned to replace the old ones. After they’re validated, they’re put into use and the old ones are decommissioned.
>
> [H. Virdó _"What Is Immutable Infrastructure?"_](https://www.digitalocean.com/community/tutorials/what-is-immutable-infrastructure)


See also: [What is Mutable vs. Immutable Infrastructure?](https://www.hashicorp.com/en/resources/what-is-mutable-vs-immutable-infrastructure)

---

## Immutable, Idempotent, Declarative IaC with Terraform

> Terraform is an infrastructure as code tool that lets you build, change, and version infrastructure safely and efficiently. This includes low-level components like compute instances, storage, and networking; and high-level components like DNS entries and SaaS features.
>
> https://developer.hashicorp.com/terraform


---

## Terraform Workflow

<img src="https://web-unified-docs-hashicorp.vercel.app/api/assets/terraform/latest/img/docs/intro-terraform-workflow.png" style="width: 40%; float:right; margin: 10px;">

> The core Terraform workflow consists of three stages:
>
> **Write**: You define resources, which may be across multiple cloud providers and services.
>
> **Plan**: Terraform creates an execution plan describing the infrastructure it will create, update, or destroy based on the existing infrastructure and your configuration.
>
> **Apply**: On approval, Terraform performs the proposed operations in the correct order, respecting any resource dependencies.
<!--
For example, if you update the properties of a VPC and change the number of virtual machines in that VPC, Terraform will recreate the VPC before scaling the virtual machines.
 -->
>
> [What is Terraform?](https://developer.hashicorp.com/terraform/intro)


<tiny>Image source: <a href="https://developer.hashicorp.com/terraform/intro">https://developer.hashicorp.com/terraform/intro</a></tiny>

---

## Terraform Workflow

<img src="https://assets.bytebytego.com/diagrams/0225-how-terraform-creates-infra-at-scale.png" width="50%">


<tiny>Image source: <a href="https://bytebytego.com/guides/how-does-terraform-turn-code-into-cloud/">https://bytebytego.com/guides/how-does-terraform-turn-code-into-cloud/</a></tiny>



## Terraform Workflow

After installing Terraform, in a directory with a Terraform IaC file, e.g., `main.tf`

```bash
terraform init
# Optional, since part of terraform plan
terraform validate
terraform plan
terraform apply
```

---

<!--
$ terraform init --help
Usage: terraform [global options] init [options]

  Initialize a new or existing Terraform working directory by creating
  initial files, loading any remote state, downloading modules, etc.

  This is the first command that should be run for any new or existing
  Terraform configuration per machine. This sets up all the local data
  necessary to run Terraform that is typically not committed to version
  control.

  This command is always safe to run multiple times. Though subsequent runs
  may give errors, this command will never delete your configuration or
  state. Even so, if you have important information, please back it up prior
  to running this command, just in case.

$ terraform validate --help
Usage: terraform [global options] validate [options]

  Validate the configuration files in a directory, referring only to the
  configuration and not accessing any remote services such as remote state,
  provider APIs, etc.

  Validate runs checks that verify whether a configuration is syntactically
  valid and internally consistent, regardless of any provided variables or
  existing state. It is thus primarily useful for general verification of
  reusable modules, including correctness of attribute names and value types.

  It is safe to run this command automatically, for example as a post-save
  check in a text editor or as a test step for a re-usable module in a CI
  system.

  Validation requires an initialized working directory with any referenced
  plugins and modules installed. To initialize a working directory for
  validation without accessing any configured remote backend, use:
      terraform init -backend=false

  To verify configuration in the context of a particular run (a particular
  target workspace, input variable values, etc), use the 'terraform plan'
  command instead, which includes an implied validation check.

$ terraform plan --help
Usage: terraform [global options] plan [options]

  Generates a speculative execution plan, showing what actions Terraform
  would take to apply the current configuration. This command will not
  actually perform the planned actions.

  You can optionally save the plan to a file, which you can then pass to
  the "apply" command to perform exactly the actions described in the plan.

$ terraform apply --help
Usage: terraform [global options] apply [options] [PLAN]

  Creates or updates infrastructure according to Terraform configuration
  files in the current directory.

  By default, Terraform will generate a new plan and present it for your
  approval before taking any action. You can optionally provide a plan
  file created by a previous call to "terraform plan", in which case
  Terraform will take the actions described in that plan without any
  confirmation prompt.

 -->


## HCL: HashiCorp Configuration Language

> Configuration files you write in Terraform language tell Terraform what plugins to install, what infrastructure to create, and what data to fetch. Terraform language also lets you define dependencies between resources and create multiple similar resources from a single configuration block.
>


--


> The main purpose of the Terraform language is declaring resources, which represent infrastructure objects. All other language features exist only to make the definition of resources more flexible and convenient.
>
> A Terraform configuration is a complete document in the Terraform language that tells Terraform how to manage a given collection of infrastructure. A configuration can consist of multiple files and directories.
>
> [Terraform Language Documentation](https://developer.hashicorp.com/terraform/language/v1.14.x)


- It is a **declarative**, **"stringly"** typed domain-specific language (DSL).
- [IDE](https://www.jetbrains.com/help/idea/terraform.html) [tooling](https://marketplace.visualstudio.com/items?itemName=hashicorp.terraform
) facilitates writing IaC in HCL.

---

## HCL Example

In the exercise session, you will create a [Docker Swarm cluster on DigitalOcean with Terraform](https://github.com/itu-devops/itu-minitwit-docker-swarm-teraform/blob/master/minitwit_swarm_cluster.tf)

```hcl
# create cloud vm
resource "digitalocean_droplet" "minitwit-swarm-leader" {
  image = "docker-20-04" // ubuntu-22-04-x64
  name = "minitwit-swarm-leader"
  region = var.region
  size = "s-1vcpu-1gb"
  # add public ssh key so we can access the machine
  ssh_keys = [digitalocean_ssh_key.minitwit.fingerprint]

  # specify a ssh connection
  connection {
    user = "root"
    host = self.ipv4_address
    type = "ssh"
    private_key = file(var.pvt_key)
    timeout = "2m"
  }

  provisioner "file" {
    source = "stack/minitwit_stack.yml"
    destination = "/root/minitwit_stack.yml"
  }

  provisioner "remote-exec" {
    inline = [
--snip--
      "ufw allow 22",

      # initialize docker swarm cluster
      "docker swarm init --advertise-addr ${self.ipv4_address}"
    ]
  }
}

resource "null_resource" "swarm-worker-token" {
  depends_on = [digitalocean_droplet.minitwit-swarm-leader]

  # save the worker join token
  provisioner "local-exec" {
    command = "ssh -o 'ConnectionAttempts 3600' -o 'StrictHostKeyChecking no' root@${digitalocean_droplet.minitwit-swarm-leader.ipv4_address} -i ssh_key/terraform 'docker swarm join-token worker -q' > temp/worker_token"
  }
}
```

---

### `terraform` block

> The terraform block allows you to configure Terraform behavior, including the Terraform version, backend, integration with HCP Terraform, and required providers.
>
> [terraform block reference](https://developer.hashicorp.com/terraform/language/block/terraform)

```hcl
# Terraform block
terraform {
  required_providers {
    digitalocean = {
      source = "digitalocean/digitalocean"
      version = "~> 2.37.1"
    }
    null = {
      source = "hashicorp/null"
      version = "3.1.0"
    }
  }
}
```

Providers can be found on the registry (> 6300 providers):

- https://registry.terraform.io/providers/digitalocean/digitalocean/latest
- https://registry.terraform.io/providers/hashicorp/null/latest

---

### Terraform `resources` block

> A resource is any infrastructure object you want to create and manage with Terraform, including virtual networks, compute instances, or higher-level components such as DNS records. The kinds of resources you can create depend on the providers you install. Providers are plugins that offer a collection of resource types that you can provision on a single cloud or on-premises infrastructure platform. Refer to Providers for more information.
>
> [Create and manage resources overview](https://developer.hashicorp.com/terraform/language/resources)

---

### Terraform `provider` block


> Terraform relies on plugins called providers to interact with cloud providers, SaaS providers, and other APIs.
>
> Terraform configurations must declare which providers they require so that Terraform can install and use them. Additionally, some providers require configuration (like endpoint URLs or cloud regions) before they can be used.
>
> Each provider adds a set of resource types and/or data sources that Terraform can manage.
>
> Every resource type is implemented by a provider; without providers, Terraform can't manage any kind of infrastructure.
>
> Most providers configure a specific infrastructure platform (either cloud or self-hosted). Providers can also offer local utilities for tasks like generating random numbers for unique resource names.
>
> [Providers](https://developer.hashicorp.com/terraform/language/providers)

```hcl
provider "aws" {
  region = "us-east-1"
}

provider "aws" {
  alias  = "west"
  region = "us-west-2"
}

resource "aws_instance" "foo" {
  provider = aws.west
  # ...
}

resource "aws_instance" "bar" {
   # ...
}
```

Providers can be found on the registry (> 6300 providers):

- https://registry.terraform.io/providers/digitalocean/digitalocean/latest
- https://registry.terraform.io/providers/hashicorp/null/latest

---

### Terraform `variable` block


> You can add variable blocks to your configuration to define input interface for your module. This lets users pass custom values to your module at runtime.
>
> Terraform sets up resources with hardcoded values the same way every time, making your module inflexible and potentially hard to reuse. If you know a value in your configuration changes between > Terraform operations, you can replace that value with a variable block. A variable block lets module consumers customize module behavior without altering the module's source code.
>
> [Use input variables to add module arguments](https://developer.hashicorp.com/terraform/language/values/variables)

--

Example, a root module resource with hardcoded values:

```hcl
resource "aws_instance" "web" {
  ami           = data.aws_ami.ubuntu.id
  instance_type = "t2.micro"
  subnet_id     = "subnet-12345"

  tags = {
    Environment = "dev"
    Name        = "dev-web-server"
  }
}
```

---

### Terraform `variable` block

```hcl
variable "instance_type" {
  type        = string
  description = "EC2 instance type for the web server"
  default     = "t2.micro"
}

variable "subnet_id" {
  type        = string
  description = "Subnet ID where the web server will be deployed"
}

variable "environment" {
  type        = string
  description = "Deployment environment name"
  default     = "dev"

  validation {
    condition     = contains(["dev", "staging", "prod"], var.environment)
    error_message = "Environment must be dev, staging, or prod."
  }
}
```

```hcl
resource "aws_instance" "web" {
  ami           = data.aws_ami.ubuntu.id
  instance_type = var.instance_type
  subnet_id     = var.subnet_id

  tags = {
    Environment = var.environment
    Name        = "${var.environment}-web-server"
  }
}
```

Source https://developer.hashicorp.com/terraform/language/values/variables

---

### Terraform: Modules

> A module is a container for multiple resources that are used together. You can use modules to create lightweight abstractions, so that you can describe your infrastructure in terms of its architecture, rather than directly in terms of physical objects.
>
> The .tf files in your working directory when you run terraform plan or terraform apply together form the root module. That module may call other modules and connect them together by passing output values from one to input values of another.
>
> [Creating Modules](https://developer.hashicorp.com/terraform/language/modules/develop)

In the exercise session, you will only use a root module

---

### Terraform State

> Terraform must store state about your managed infrastructure and configuration. This state is used by Terraform to map real world resources to your configuration, keep track of metadata, and to improve performance for large infrastructures.
>
> This state is stored by default in a local file named "terraform.tfstate", but we recommend storing it in HCP Terraform to version, encrypt, and securely share it with your team.
>
> Terraform uses state to determine which changes to make to your infrastructure. Prior to any operation, Terraform does a refresh to update the state with the real infrastructure.
>
> [State](https://developer.hashicorp.com/terraform/language/state)

---

### Example: Provisioning a Docker Swarm Cluster on DigitalOcean with Terraform

In your exercise session, you will provision a Docker Swarm cluster on DigitalOcean with Terraform

<img src="https://github.com/itu-devops/itu-minitwit-docker-swarm-teraform/blob/master/images/diagram.png?raw=true" width="60%">

---

### Example: Provisioning a Docker Swarm Cluster on DigitalOcean with Terraform

- [`provider.tf`](https://github.com/itu-devops/itu-minitwit-docker-swarm-teraform/blob/master/provider.tf)
  - Configuration of DigitalOcean and null provider
  - Configuration of DigitalOcean's API token via a variable

```hcl
provider "digitalocean" {
  token = var.do_token
}
```

- Is filled with the value from environment variable, which is set by `source secrets` in the [`bootstrap.sh` script](https://github.com/itu-devops/itu-minitwit-docker-swarm-teraform/blob/6e9bb64b5f7071090ba4034f6e6b33c13efe8891/bootstrap.sh#L8)
- All environment variables that start on `TF_VAR_` are automatically available on HCL files.

---

### Example: Provisioning a Docker Swarm Cluster on DigitalOcean with Terraform

- [`minitwit.auto.tfvars`](https://github.com/itu-devops/itu-minitwit-docker-swarm-teraform/blob/master/minitwit.auto.tfvars)
  - Automatically-loaded variables, indicated by  `.auto.tfvars` extension
  - Contains Terraform variable values, e.g., region and location of private and public SSH keys
  - SSH key pair is [generated manually in exercise tutorial](https://github.com/itu-devops/itu-minitwit-docker-swarm-teraform/blob/6e9bb64b5f7071090ba4034f6e6b33c13efe8891/README.md?plain=1#L44)

---

### Example: Provisioning a Docker Swarm Cluster on DigitalOcean with Terraform

- [`ssh_key.tf`](https://github.com/itu-devops/itu-minitwit-docker-swarm-teraform/blob/master/ssh_key.tf)
  - Terraform configuration file defining a DigitalOcean SSH key resource
  - It shares the public SSH key originating from a file and stored in a TF variable `var.pub_key`

---

### Example: Provisioning a Docker Swarm Cluster on DigitalOcean with Terraform

- [`backend.tf`](https://github.com/itu-devops/itu-minitwit-docker-swarm-teraform/blob/master/backend.tf)
  - Configures remote AWS S3-compatible storage (DigitalOcean Spaces)
  - Used to share Terraform state, e.g., across team members or CI/CD pipelines
  - To avoid clashing states when `terraform apply` from different computers.

---

### Example: Provisioning a Docker Swarm Cluster on DigitalOcean with Terraform


- [`ip.tf`](https://github.com/itu-devops/itu-minitwit-docker-swarm-teraform/blob/master/ip.tf)
  - Configure floating IP (via resource)
  - Assign it to the swarm cluster leader node
  - Remember, this is the static public IP address for accessing the Docker Swarm cluster regardless of underlying droplet changes

---

### Example: Provisioning a Docker Swarm Cluster on DigitalOcean with Terraform

- [`minitwit_swarm_cluster.tf`](https://github.com/itu-devops/itu-minitwit-docker-swarm-teraform/blob/master/minitwit_swarm_cluster.tf)
  - Core infrastructure definition file.
  - Creates and configures the Docker Swarm cluster on DigitalOcean
  - Three managers (one leader), and three workers.
  - It also handles initialization of the swarm cluster and deployment of the minitwit application stack.

---

### Terraform Resource Dependency Graph

```bash
terraform graph
```

<img src="images/swarm_tf_diag.png" width="70%">

<!--
<img src="images/tf_resource_dep_graph.png" width="100%">

 -->

---
<!--
### Terraform Resource Dependency Graph

- Explicit Dependencies (from ): `depends_on`
1. `null_resource.swarm-worker-token` depends on `digitalocean_droplet.minitwit-swarm-leader`
2. `null_resource.swarm-manager-token` depends on `digitalocean_droplet.minitwit-swarm-leader`
3. `digitalocean_droplet.minitwit-swarm-manager` depends on `null_resource.swarm-manager-token`
4. `digitalocean_droplet.minitwit-swarm-worker` depends on `null_resource.swarm-worker-token`

- Implicit Dependencies (from resource references):
1. All droplets depend on `digitalocean_ssh_key.minitwit` (via attribute) `ssh_keys`
2. `digitalocean_floating_ip_assignment.public-ip-assignment` depends on:
    - `digitalocean_floating_ip.public-ip`
    - `digitalocean_droplet.minitwit-swarm-leader`

- Terraform will create resources in this order:
1. `digitalocean_ssh_key.minitwit``digitalocean_floating_ip.public-ip`
2. `digitalocean_droplet.minitwit-swarm-leader`
3. `digitalocean_floating_ip_assignment.public-ip-assignment` `null_resource.swarm-worker-token``null_resource.swarm-manager-token`
4. `digitalocean_droplet.minitwit-swarm-manager``digitalocean_droplet.minitwit-swarm-worker`

This ensures that:
- SSH keys exist before any droplets are created
- The swarm leader initializes before tokens are extracted
- Manager/worker nodes only join after their respective tokens are available
 -->

### OpenTofu vs. Terraform

<img src="https://miro.medium.com/v2/resize:fit:720/format:webp/1*D9Uw7VTZibb2ws2MgbJXLA.png", width="30%">

- [OpenTofu](https://opentofu.org/)
- [Terraform](https://developer.hashicorp.com/terraform)

> Why was OpenTofu created?
>
> OpenTofu is a Terraform fork, created as an initiative of Gruntwork, Spacelift, Harness, Env0, Scalr, and others, in response to HashiCorp's switch from an open-source license to the BUSL. The initiative has many supporters, all of whom are listed here.
>
> The BUSL and the additional use grant outlined by the HashiCorp team are ambiguous, which makes it challenging for companies, vendors, and developers using Terraform to decide whether their actions could be interpreted as being outside the permitted scope of use.
> https://opentofu.org/faq/

---

## What to do now?

  * To experiment and practice the topics of today, conduct the [exercise tasks](./README_EXERCISE.md)
  * Optionally, do the [project work](./README_TASKS.md) until the end of the week

---

## Advertisment

<img src="images/hackathon_ad.png" width="100%">

https://www.royalhacks.io/